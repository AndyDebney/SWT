sources:
  - name: raw
    description: Raw operational tables from ADEBNEYDEMODB4.RAW.
    database: ADEBNEYDEMODB4
    schema: RAW
    tables:
      - name: brands
        description: Raw brand records.
        columns:
          - name: _file
            description: Source column `_FILE` (TEXT).
          - name: _line
            description: Source column `_LINE` (NUMBER).
          - name: _modified
            description: Source column `_MODIFIED` (TIMESTAMP_TZ).
          - name: _fivetran_synced
            description: Source column `_FIVETRAN_SYNCED` (TIMESTAMP_TZ).
          - name: brand_id
            description: Source column `BRAND_ID` (NUMBER).
          - name: brand_name
            description: Source column `BRAND_NAME` (TEXT).

      - name: categories
        description: Raw product category records.
        columns:
          - name: _file
            description: Source column `_FILE` (TEXT).
          - name: _line
            description: Source column `_LINE` (NUMBER).
          - name: _modified
            description: Source column `_MODIFIED` (TIMESTAMP_TZ).
          - name: _fivetran_synced
            description: Source column `_FIVETRAN_SYNCED` (TIMESTAMP_TZ).
          - name: category_id
            description: Source column `CATEGORY_ID` (NUMBER).
          - name: category_name
            description: Source column `CATEGORY_NAME` (TEXT).

      - name: customers
        description: Raw customer records.
        columns:
          - name: _file
            description: Source column `_FILE` (TEXT).
          - name: _line
            description: Source column `_LINE` (NUMBER).
          - name: _modified
            description: Source column `_MODIFIED` (TIMESTAMP_TZ).
          - name: _fivetran_synced
            description: Source column `_FIVETRAN_SYNCED` (TIMESTAMP_TZ).
          - name: customer_id
            description: Source column `CUSTOMER_ID` (NUMBER).
          - name: first_name
            description: Source column `FIRST_NAME` (TEXT).
          - name: last_name
            description: Source column `LAST_NAME` (TEXT).
          - name: phone
            description: Source column `PHONE` (TEXT).
          - name: email
            description: Source column `EMAIL` (TEXT).
          - name: street
            description: Source column `STREET` (TEXT).
          - name: city
            description: Source column `CITY` (TEXT).
          - name: state
            description: Source column `STATE` (TEXT).
          - name: zip_code
            description: Source column `ZIP_CODE` (NUMBER).

      - name: orders
        description: Raw order header records.
        columns:
          - name: _file
            description: Source column `_FILE` (TEXT).
          - name: _line
            description: Source column `_LINE` (NUMBER).
          - name: _modified
            description: Source column `_MODIFIED` (TIMESTAMP_TZ).
          - name: _fivetran_synced
            description: Source column `_FIVETRAN_SYNCED` (TIMESTAMP_TZ).
          - name: order_id
            description: Source column `ORDER_ID` (NUMBER).
          - name: customer_id
            description: Source column `CUSTOMER_ID` (NUMBER).
          - name: order_status
            description: Source column `ORDER_STATUS` (NUMBER).
          - name: order_date
            description: Source column `ORDER_DATE` (DATE).
          - name: required_date
            description: Source column `REQUIRED_DATE` (DATE).
          - name: shipped_date
            description: Source column `SHIPPED_DATE` (TEXT).
          - name: store_id
            description: Source column `STORE_ID` (NUMBER).
          - name: staff_id
            description: Source column `STAFF_ID` (NUMBER).

      - name: order_items
        description: Raw line-item records for orders.
        columns:
          - name: _file
            description: Source column `_FILE` (TEXT).
          - name: _line
            description: Source column `_LINE` (NUMBER).
          - name: _modified
            description: Source column `_MODIFIED` (TIMESTAMP_TZ).
          - name: _fivetran_synced
            description: Source column `_FIVETRAN_SYNCED` (TIMESTAMP_TZ).
          - name: order_id
            description: Source column `ORDER_ID` (NUMBER).
          - name: item_id
            description: Source column `ITEM_ID` (NUMBER).
          - name: product_id
            description: Source column `PRODUCT_ID` (NUMBER).
          - name: quantity
            description: Source column `QUANTITY` (NUMBER).
          - name: list_price
            description: Source column `LIST_PRICE` (FLOAT).
          - name: discount
            description: Source column `DISCOUNT` (FLOAT).

      - name: products
        description: Raw product records.
        columns:
          - name: _file
            description: Source column `_FILE` (TEXT).
          - name: _line
            description: Source column `_LINE` (NUMBER).
          - name: _modified
            description: Source column `_MODIFIED` (TIMESTAMP_TZ).
          - name: _fivetran_synced
            description: Source column `_FIVETRAN_SYNCED` (TIMESTAMP_TZ).
          - name: product_id
            description: Source column `PRODUCT_ID` (NUMBER).
          - name: product_name
            description: Source column `PRODUCT_NAME` (TEXT).
          - name: brand_id
            description: Source column `BRAND_ID` (NUMBER).
          - name: category_id
            description: Source column `CATEGORY_ID` (NUMBER).
          - name: model_year
            description: Source column `MODEL_YEAR` (NUMBER).
          - name: list_price
            description: Source column `LIST_PRICE` (FLOAT).

      - name: staffs
        description: Raw staff records.
        columns:
          - name: _file
            description: Source column `_FILE` (TEXT).
          - name: _line
            description: Source column `_LINE` (NUMBER).
          - name: _modified
            description: Source column `_MODIFIED` (TIMESTAMP_TZ).
          - name: _fivetran_synced
            description: Source column `_FIVETRAN_SYNCED` (TIMESTAMP_TZ).
          - name: staff_id
            description: Source column `STAFF_ID` (NUMBER).
          - name: first_name
            description: Source column `FIRST_NAME` (TEXT).
          - name: last_name
            description: Source column `LAST_NAME` (TEXT).
          - name: email
            description: Source column `EMAIL` (TEXT).
          - name: phone
            description: Source column `PHONE` (TEXT).
          - name: active
            description: Source column `ACTIVE` (NUMBER).
          - name: store_id
            description: Source column `STORE_ID` (NUMBER).
          - name: manager_id
            description: Source column `MANAGER_ID` (TEXT).

      - name: stocks
        description: Raw inventory stock records.
        columns:
          - name: _file
            description: Source column `_FILE` (TEXT).
          - name: _line
            description: Source column `_LINE` (NUMBER).
          - name: _modified
            description: Source column `_MODIFIED` (TIMESTAMP_TZ).
          - name: _fivetran_synced
            description: Source column `_FIVETRAN_SYNCED` (TIMESTAMP_TZ).
          - name: store_id
            description: Source column `STORE_ID` (NUMBER).
          - name: product_id
            description: Source column `PRODUCT_ID` (NUMBER).
          - name: quantity
            description: Source column `QUANTITY` (NUMBER).

      - name: stores
        description: Raw store records.
        columns:
          - name: _file
            description: Source column `_FILE` (TEXT).
          - name: _line
            description: Source column `_LINE` (NUMBER).
          - name: _modified
            description: Source column `_MODIFIED` (TIMESTAMP_TZ).
          - name: _fivetran_synced
            description: Source column `_FIVETRAN_SYNCED` (TIMESTAMP_TZ).
          - name: store_id
            description: Source column `STORE_ID` (NUMBER).
          - name: store_name
            description: Source column `STORE_NAME` (TEXT).
          - name: phone
            description: Source column `PHONE` (TEXT).
          - name: email
            description: Source column `EMAIL` (TEXT).
          - name: street
            description: Source column `STREET` (TEXT).
          - name: city
            description: Source column `CITY` (TEXT).
          - name: state
            description: Source column `STATE` (TEXT).
          - name: zip_code
            description: Source column `ZIP_CODE` (NUMBER).


      - name: customer_support_interactions
        description: Raw customer support interaction records.
        columns:
          - name: _file
            description: Source column `_FILE` (TEXT).
          - name: _line
            description: Source column `_LINE` (NUMBER).
          - name: _modified
            description: Source column `_MODIFIED` (TIMESTAMP_TZ).
          - name: _fivetran_synced
            description: Source column `_FIVETRAN_SYNCED` (TIMESTAMP_TZ).
          - name: interaction_id
            description: Source column `INTERACTION_ID` (TEXT).
          - name: customer_id
            description: Source column `CUSTOMER_ID` (NUMBER).
          - name: order_id
            description: Source column `ORDER_ID` (NUMBER).
          - name: product_id
            description: Source column `PRODUCT_ID` (NUMBER).
          - name: interaction_at
            description: Source column `INTERACTION_AT` (TIMESTAMP_NTZ).
          - name: channel
            description: Source column `CHANNEL` (TEXT).
          - name: interaction_type
            description: Source column `INTERACTION_TYPE` (TEXT).
          - name: issue_category
            description: Source column `ISSUE_CATEGORY` (TEXT).
          - name: sentiment_score
            description: Source column `SENTIMENT_SCORE` (FLOAT).
          - name: sentiment_label
            description: Source column `SENTIMENT_LABEL` (TEXT).
          - name: csat_score
            description: Source column `CSAT_SCORE` (NUMBER).
          - name: resolved_flag
            description: Source column `RESOLVED_FLAG` (BOOLEAN).
          - name: resolution_minutes
            description: Source column `RESOLUTION_MINUTES` (NUMBER).
          - name: follow_up_required
            description: Source column `FOLLOW_UP_REQUIRED` (BOOLEAN).
          - name: interaction_cost
            description: Source column `INTERACTION_COST` (FLOAT).
          - name: refund_amount
            description: Source column `REFUND_AMOUNT` (FLOAT).
          - name: support_agent_id
            description: Source column `SUPPORT_AGENT_ID` (TEXT).
          - name: interaction_summary
            description: Source column `INTERACTION_SUMMARY` (TEXT).
