with source_data as (
    select
        _file,
        _line,
        _modified,
        _fivetran_synced,
        interaction_id,
        customer_id,
        order_id,
        product_id,
        interaction_at,
        channel,
        interaction_type,
        issue_category,
        sentiment_score,
        sentiment_label,
        csat_score,
        resolved_flag,
        resolution_minutes,
        follow_up_required,
        interaction_cost,
        refund_amount,
        support_agent_id,
        interaction_summary
    from {{ source('raw', 'customer_support_interactions') }}
)

select
    _file,
    _line,
    _modified,
    _fivetran_synced,
    interaction_id,
    customer_id,
    order_id,
    product_id,
    interaction_at,
    channel,
    interaction_type,
    issue_category,
    sentiment_score,
    sentiment_label,
    csat_score,
    resolved_flag,
    resolution_minutes,
    follow_up_required,
    interaction_cost,
    refund_amount,
    support_agent_id,
    interaction_summary
from source_data
