version: 2

models:
  - name: int_brands
    description: Intermediate pass-through model built from `stg_brands`, with standardized audit column names.
    columns:
      - name: source_file
        description: Source file path or name from the upstream staging model.
      - name: record_line_no
        description: Source record line number from the upstream staging model.
      - name: updated_at
        description: Source record modification timestamp from the upstream staging model.
      - name: loaded_at
        description: Source load timestamp from the upstream staging model.
      - name: brand_id
        description: Raw `brand_id` value carried forward from `stg_brands`.
        data_tests:
          - not_null
          - unique
      - name: brand_name
        description: Raw `brand_name` value carried forward from `stg_brands`.

  - name: int_categories
    description: Intermediate pass-through model built from `stg_categories`, with standardized audit column names.
    columns:
      - name: source_file
        description: Source file path or name from the upstream staging model.
      - name: record_line_no
        description: Source record line number from the upstream staging model.
      - name: updated_at
        description: Source record modification timestamp from the upstream staging model.
      - name: loaded_at
        description: Source load timestamp from the upstream staging model.
      - name: category_id
        description: Raw `category_id` value carried forward from `stg_categories`.
        data_tests:
          - not_null
          - unique
      - name: category_name
        description: Raw `category_name` value carried forward from `stg_categories`.

  - name: int_customers
    description: Intermediate pass-through model built from `stg_customers`, with standardized audit column names.
    columns:
      - name: source_file
        description: Source file path or name from the upstream staging model.
      - name: record_line_no
        description: Source record line number from the upstream staging model.
      - name: updated_at
        description: Source record modification timestamp from the upstream staging model.
      - name: loaded_at
        description: Source load timestamp from the upstream staging model.
      - name: customer_id
        description: Raw `customer_id` value carried forward from `stg_customers`.
        data_tests:
          - not_null
          - unique
      - name: first_name
        description: Raw `first_name` value carried forward from `stg_customers`.
      - name: last_name
        description: Raw `last_name` value carried forward from `stg_customers`.
      - name: phone
        description: Raw `phone` value carried forward from `stg_customers`.
      - name: email
        description: Raw `email` value carried forward from `stg_customers`.
      - name: street
        description: Raw `street` value carried forward from `stg_customers`.
      - name: city
        description: Raw `city` value carried forward from `stg_customers`.
      - name: state
        description: Raw `state` value carried forward from `stg_customers`.
      - name: zip_code
        description: Raw `zip_code` value carried forward from `stg_customers`.

  - name: int_customer_support_interactions
    description: Intermediate pass-through model built from `stg_customer_support_interactions`, with standardized audit column names.
    columns:
      - name: source_file
        description: Source file path or name from the upstream staging model.
      - name: record_line_no
        description: Source record line number from the upstream staging model.
      - name: updated_at
        description: Source record modification timestamp from the upstream staging model.
      - name: loaded_at
        description: Source load timestamp from the upstream staging model.
      - name: interaction_id
        description: Raw `interaction_id` value carried forward from `stg_customer_support_interactions`.
        data_tests:
          - not_null
          - unique
      - name: customer_id
        description: Raw `customer_id` value carried forward from `stg_customer_support_interactions`.
      - name: order_id
        description: Raw `order_id` value carried forward from `stg_customer_support_interactions`.
      - name: product_id
        description: Raw `product_id` value carried forward from `stg_customer_support_interactions`.
      - name: interaction_at
        description: Raw `interaction_at` value carried forward from `stg_customer_support_interactions`.
      - name: channel
        description: Raw `channel` value carried forward from `stg_customer_support_interactions`.
      - name: interaction_type
        description: Raw `interaction_type` value carried forward from `stg_customer_support_interactions`.
      - name: issue_category
        description: Raw `issue_category` value carried forward from `stg_customer_support_interactions`.
      - name: sentiment_score
        description: Raw `sentiment_score` value carried forward from `stg_customer_support_interactions`.
      - name: sentiment_label
        description: Raw `sentiment_label` value carried forward from `stg_customer_support_interactions`.
      - name: csat_score
        description: Raw `csat_score` value carried forward from `stg_customer_support_interactions`.
      - name: resolved_flag
        description: Raw `resolved_flag` value carried forward from `stg_customer_support_interactions`.
      - name: resolution_minutes
        description: Raw `resolution_minutes` value carried forward from `stg_customer_support_interactions`.
      - name: follow_up_required
        description: Raw `follow_up_required` value carried forward from `stg_customer_support_interactions`.
      - name: interaction_cost
        description: Raw `interaction_cost` value carried forward from `stg_customer_support_interactions`.
      - name: refund_amount
        description: Raw `refund_amount` value carried forward from `stg_customer_support_interactions`.
      - name: support_agent_id
        description: Raw `support_agent_id` value carried forward from `stg_customer_support_interactions`.
      - name: interaction_summary
        description: Raw `interaction_summary` value carried forward from `stg_customer_support_interactions`.

  - name: int_orders
    description: Intermediate pass-through model built from `stg_orders`, with standardized audit column names.
    columns:
      - name: source_file
        description: Source file path or name from the upstream staging model.
      - name: record_line_no
        description: Source record line number from the upstream staging model.
      - name: updated_at
        description: Source record modification timestamp from the upstream staging model.
      - name: loaded_at
        description: Source load timestamp from the upstream staging model.
      - name: order_id
        description: Raw `order_id` value carried forward from `stg_orders`.
        data_tests:
          - not_null
          - unique
      - name: customer_id
        description: Raw `customer_id` value carried forward from `stg_orders`.
      - name: order_status
        description: Raw `order_status` value carried forward from `stg_orders`.
      - name: order_date
        description: Raw `order_date` value carried forward from `stg_orders`.
      - name: required_date
        description: Raw `required_date` value carried forward from `stg_orders`.
      - name: shipped_date
        description: Raw `shipped_date` value carried forward from `stg_orders`.
      - name: store_id
        description: Raw `store_id` value carried forward from `stg_orders`.
      - name: staff_id
        description: Raw `staff_id` value carried forward from `stg_orders`.

  - name: int_order_items
    description: Intermediate pass-through model built from `stg_order_items`, with standardized audit column names.
    columns:
      - name: source_file
        description: Source file path or name from the upstream staging model.
      - name: record_line_no
        description: Source record line number from the upstream staging model.
      - name: updated_at
        description: Source record modification timestamp from the upstream staging model.
      - name: loaded_at
        description: Source load timestamp from the upstream staging model.
      - name: order_id
        description: Raw `order_id` value carried forward from `stg_order_items`.
        data_tests:
          - not_null
      - name: item_id
        description: Raw `item_id` value carried forward from `stg_order_items`.
        data_tests:
          - not_null
      - name: product_id
        description: Raw `product_id` value carried forward from `stg_order_items`.
        data_tests:
          - not_null
      - name: quantity
        description: Raw `quantity` value carried forward from `stg_order_items`.
      - name: list_price
        description: Raw `list_price` value carried forward from `stg_order_items`.
      - name: discount
        description: Raw `discount` value carried forward from `stg_order_items`.

  - name: int_products
    description: Intermediate pass-through model built from `stg_products`, with standardized audit column names.
    columns:
      - name: source_file
        description: Source file path or name from the upstream staging model.
      - name: record_line_no
        description: Source record line number from the upstream staging model.
      - name: updated_at
        description: Source record modification timestamp from the upstream staging model.
      - name: loaded_at
        description: Source load timestamp from the upstream staging model.
      - name: product_id
        description: Raw `product_id` value carried forward from `stg_products`.
        data_tests:
          - not_null
          - unique
      - name: product_name
        description: Raw `product_name` value carried forward from `stg_products`.
      - name: brand_id
        description: Raw `brand_id` value carried forward from `stg_products`.
      - name: category_id
        description: Raw `category_id` value carried forward from `stg_products`.
      - name: model_year
        description: Raw `model_year` value carried forward from `stg_products`.
      - name: list_price
        description: Raw `list_price` value carried forward from `stg_products`.

  - name: int_staffs
    description: Intermediate pass-through model built from `stg_staffs`, with standardized audit column names.
    columns:
      - name: source_file
        description: Source file path or name from the upstream staging model.
      - name: record_line_no
        description: Source record line number from the upstream staging model.
      - name: updated_at
        description: Source record modification timestamp from the upstream staging model.
      - name: loaded_at
        description: Source load timestamp from the upstream staging model.
      - name: staff_id
        description: Raw `staff_id` value carried forward from `stg_staffs`.
        data_tests:
          - not_null
          - unique
      - name: first_name
        description: Raw `first_name` value carried forward from `stg_staffs`.
      - name: last_name
        description: Raw `last_name` value carried forward from `stg_staffs`.
      - name: email
        description: Raw `email` value carried forward from `stg_staffs`.
      - name: phone
        description: Raw `phone` value carried forward from `stg_staffs`.
      - name: active
        description: Raw `active` value carried forward from `stg_staffs`.
      - name: store_id
        description: Raw `store_id` value carried forward from `stg_staffs`.
      - name: manager_id
        description: Raw `manager_id` value carried forward from `stg_staffs`.

  - name: int_stocks
    description: Intermediate pass-through model built from `stg_stocks`, with standardized audit column names.
    columns:
      - name: source_file
        description: Source file path or name from the upstream staging model.
      - name: record_line_no
        description: Source record line number from the upstream staging model.
      - name: updated_at
        description: Source record modification timestamp from the upstream staging model.
      - name: loaded_at
        description: Source load timestamp from the upstream staging model.
      - name: store_id
        description: Raw `store_id` value carried forward from `stg_stocks`.
        data_tests:
          - not_null
      - name: product_id
        description: Raw `product_id` value carried forward from `stg_stocks`.
        data_tests:
          - not_null
      - name: quantity
        description: Raw `quantity` value carried forward from `stg_stocks`.

  - name: int_stores
    description: Intermediate pass-through model built from `stg_stores`, with standardized audit column names.
    columns:
      - name: source_file
        description: Source file path or name from the upstream staging model.
      - name: record_line_no
        description: Source record line number from the upstream staging model.
      - name: updated_at
        description: Source record modification timestamp from the upstream staging model.
      - name: loaded_at
        description: Source load timestamp from the upstream staging model.
      - name: store_id
        description: Raw `store_id` value carried forward from `stg_stores`.
        data_tests:
          - not_null
          - unique
      - name: store_name
        description: Raw `store_name` value carried forward from `stg_stores`.
      - name: phone
        description: Raw `phone` value carried forward from `stg_stores`.
      - name: email
        description: Raw `email` value carried forward from `stg_stores`.
      - name: street
        description: Raw `street` value carried forward from `stg_stores`.
      - name: city
        description: Raw `city` value carried forward from `stg_stores`.
      - name: state
        description: Raw `state` value carried forward from `stg_stores`.
      - name: zip_code
        description: Raw `zip_code` value carried forward from `stg_stores`.
